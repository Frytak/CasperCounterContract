[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-engine-test-support = { version = "5.0.0", features = ["test-support"] }
casper-execution-engine = "5.0.0"
casper-types = "3.0.0"

[[bin]]
name = "tests"
path = "src/tests.rs"
bench = false
doctest = false
